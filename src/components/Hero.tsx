import Navbar from "./Navbar";

const Hero = () => {
  return (
    <div
      id="hero-section"
      className="flex flex-col items-center min-h-screen bg-text dark:bg-bg-dark"
    >
      <Navbar />
    </div>
  );
};

export default Hero;
